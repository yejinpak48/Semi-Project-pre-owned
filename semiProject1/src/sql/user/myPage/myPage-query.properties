selectMyPageList=SELECT RNUM, ORDER_NO, PRODUCT_NAME, ORDER_DATE, DELIVERY_STATUS, REFUND_STATUS, PAY_STATUS, MEMBER_NO, RANK FROM (SELECT ROWNUM RNUM, OD.ORDER_NO, P.PRODUCT_NAME, OP.ORDER_DATE, DD.DELIVERY_STATUS, PD.REFUND_STATUS, PD.PAY_STATUS, OP.MEMBER_NO, ROW_NUMBER() OVER (ORDER BY ORDER_DATE DESC) RANK FROM ORDER_DETAIL OD JOIN ORDER_PRODUCT OP ON (OD.ORDER_NO = OP.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN DELIVERY D ON (D.ORDER_NO = OP.ORDER_NO) JOIN DELIVERY_DETAIL DD ON (DD.DELIVERY_NO = D.DELIVERY_NO) JOIN PAY PA ON (OD.ORDER_NO = PA.ORDER_NO) JOIN PAY_DETAIL PD ON (PA.PAY_NO = PD.PAY_NO) WHERE OP.MEMBER_NO = ? AND ORDER_DATE BETWEEN ADD_MONTHS(SYSDATE, -1) AND SYSDATE) WHERE RANK BETWEEN ? AND ?
selectMyPageListCount=SELECT COUNT(*) FROM (SELECT ORDER_NO, PRODUCT_NAME, ORDER_DATE, DELIVERY_STATUS FROM ORDER_DETAIL OD JOIN ORDER_PRODUCT OP ON (OD.ORDER_NO = OP.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN DELIVERY D ON (D.ORDER_NO = OP.ORDER_NO) JOIN DELIVERY_DETAIL DD ON (DD.DELIVERY_NO = D.DELIVERY_NO) WHERE MEMBER_NO = ? AND ORDER_DATE BETWEEN ADD_MONTHS(SYSDATE, -1) AND SYSDATE)
selectDeliReadyList=SELECT ORDER_NO, PRODUCT_NAME, ORDER_DATE, DELIVERY_STATUS FROM ORDER_DETAIL OD JOIN ORDER_PRODUCT OP ON (OD.ORDER_NO = OP.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN DELIVERY D ON (D.ORDER_NO = OP.ORDER_NO) JOIN DELIVERY_DETAIL DD ON (DD.DELIVERY_NO = D.DELIVERY_NO) WHERE MEMBER_NO = ? AND ORDER_DATE BETWEEN ADD_MONTHS(SYSDATE, -1) AND SYSDATE
selectOrderLook=SELECT ORDER_DATE, ORDER_NO, PRODUCT_NAME, PAY_STATUS, DELIVERY_STATUS, REFUND_STATUS, MEMBER_NO, RANK FROM(SELECT ROWNUM RNUM, OP.ORDER_DATE, OP.ORDER_NO, P.PRODUCT_NAME, PD.PAY_STATUS, DD.DELIVERY_STATUS, PD.REFUND_STATUS, OP.MEMBER_NO, ROW_NUMBER() OVER (ORDER BY ORDER_DATE DESC) RANK FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PAY PA ON (PA.ORDER_NO = OD.ORDER_NO) JOIN PAY_DETAIL PD ON (PD.PAY_NO = PA.PAY_NO) JOIN PRODUCT P ON (P.PRODUCT_CODE = OD.PRODUCT_CODE) LEFT OUTER JOIN DELIVERY D ON (D.ORDER_NO = OP.ORDER_NO) LEFT OUTER JOIN DELIVERY_DETAIL DD ON (DD.DELIVERY_NO = D.DELIVERY_NO) WHERE OP.MEMBER_NO = ?) WHERE RANK BETWEEN ? AND ?
selectOrderLookCount=SELECT COUNT(*) FROM (SELECT OP.ORDER_DATE, OP.ORDER_NO, P.PRODUCT_NAME, PD.PAY_STATUS, DD.DELIVERY_STATUS, PD.REFUND_STATUS, OP.MEMBER_NO FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PAY PA ON (PA.ORDER_NO = OD.ORDER_NO) JOIN PAY_DETAIL PD ON (PD.PAY_NO = PA.PAY_NO) JOIN PRODUCT P ON (P.PRODUCT_CODE = OD.PRODUCT_CODE) LEFT OUTER JOIN DELIVERY D ON (D.ORDER_NO = OP.ORDER_NO) LEFT OUTER JOIN DELIVERY_DETAIL DD ON (DD.DELIVERY_NO = D.DELIVERY_NO) WHERE OP.MEMBER_NO = ?)
selectOrderDetail=SELECT OD.PRODUCT_DTPAY, OP.ORDER_DATE, OP.ORDER_NO, P.PRODUCT_NAME, PD.PAY_STATUS, PD.REFUND_STATUS, DD.DELIVERY_STATUS, DD.INVOICE_NO, DS.RECIEVER_NAME, DS.DELIVERY_SITE, DS.RECIEVER_PHONE, D.MEMBER_NO, PC.DELIVERY_PAY, DD.COURIER_NAME, PD.REFUND_DATE FROM PAY_DETAIL PD, PAY PA, ORDER_PRODUCT OP, ORDER_DETAIL OD, PRODUCT P, DELIVERY D, DELIVERY_SITE DS , DELIVERY_DETAIL DD, PRODUCT_CHECK PC WHERE P.PRODUCT_CODE = OD.PRODUCT_CODE AND OD.ORDER_NO = OP.ORDER_NO AND OP.ORDER_NO = PA.ORDER_NO AND PA.ORDER_NO = D.ORDER_NO AND D.DELIVERY_NO = DD.DELIVERY_NO AND D.DELIVERY_SITE_CODE = DS.DELIVERY_SITE_NO AND PA.PAY_NO = PD.PAY_NO AND P.PRODUCT_CODE = PC.PRODUCT_CODE AND PA.ORDER_NO = ? AND D.MEMBER_NO = ?
selectOrderDate=SELECT ORDER_DATE, ORDER_NO, PRODUCT_NAME, PAY_STATUS, DELIVERY_STATUS, REFUND_STATUS, MEMBER_NO, RANK FROM (SELECT ROWNUM RNUM, OP.ORDER_DATE, OP.ORDER_NO, P.PRODUCT_NAME, PD.PAY_STATUS, DELIVERY_STATUS, PD.REFUND_STATUS, OP.MEMBER_NO, ROW_NUMBER() OVER (ORDER BY ORDER_DATE DESC) RANK FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PAY PA ON (PA.ORDER_NO = OD.ORDER_NO) JOIN PAY_DETAIL PD ON (PD.PAY_NO = PA.PAY_NO) JOIN PRODUCT P ON (P.PRODUCT_CODE = OD.PRODUCT_CODE) LEFT OUTER JOIN DELIVERY D ON (D.ORDER_NO = OP.ORDER_NO) LEFT OUTER JOIN DELIVERY_DETAIL DD ON (DD.DELIVERY_NO = D.DELIVERY_NO) WHERE OP.MEMBER_NO = ? AND ORDER_DATE BETWEEN TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND TO_CHAR(TO_DATE(?,'YYYY-MM-DD'))) WHERE RANK BETWEEN ? AND ?
selectOrderDateCount=SELECT COUNT(*) FROM (SELECT OP.ORDER_DATE, OP.ORDER_NO, P.PRODUCT_NAME, PD.PAY_STATUS, DD.DELIVERY_STATUS, PD.REFUND_STATUS, OP.MEMBER_NO FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PAY PA ON (PA.ORDER_NO = OD.ORDER_NO) JOIN PAY_DETAIL PD ON (PD.PAY_NO = PA.PAY_NO) JOIN PRODUCT P ON (P.PRODUCT_CODE = OD.PRODUCT_CODE) LEFT OUTER JOIN DELIVERY D ON (D.ORDER_NO = OP.ORDER_NO) LEFT OUTER JOIN DELIVERY_DETAIL DD ON (DD.DELIVERY_NO = D.DELIVERY_NO) WHERE OP.MEMBER_NO = ? AND ORDER_DATE BETWEEN TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND TO_CHAR(TO_DATE(?,'YYYY-MM-DD')))

selectRefundList=SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, REFUND_STATUS, RANK, MEMBER_NO FROM (SELECT OD.ORDER_NO, PD.REFUND_DATE, P.PRODUCT_NAME, OD.PRODUCT_DTPAY, PD.REFUND_STATUS, ROW_NUMBER() OVER (ORDER BY REFUND_DATE DESC) RANK, PA.MEMBER_NO FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN PAY PA ON (OD.ORDER_NO = PA.ORDER_NO) JOIN PAY_DETAIL PD ON (PA.PAY_NO = PD.PAY_NO) WHERE PA.MEMBER_NO = ? AND REFUND_STATUS IS NOT NULL) WHERE RANK BETWEEN ? AND ?
selectCalculateList=SELECT ADJUST_NO, ADJUST_DATE, ADJUST_DIV, PRICE, ACCOUNT_HOLDER, BANK_CODE, ACCOUNT_NO, MEMBER_NO, RANK FROM (SELECT ADJUST_NO, ADJUST_DATE, ADJUST_DIV, PRICE, ACCOUNT_HOLDER, BANK_CODE, ACCOUNT_NO, A.MEMBER_NO, ROW_NUMBER() OVER (ORDER BY ADJUST_DATE DESC) RANK FROM ADJUSTMENT A JOIN MEMBER M ON (A.MEMBER_NO = M.MEMBER_NO) WHERE A.MEMBER_NO = ?) WHERE RANK BETWEEN ? AND ?
selectListCountR=SELECT COUNT(*) FROM (SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, REFUND_STATUS, RANK, MEMBER_NO FROM (SELECT OD.ORDER_NO, PD.REFUND_DATE, P.PRODUCT_NAME, OD.PRODUCT_DTPAY, PD.REFUND_STATUS, ROW_NUMBER() OVER (ORDER BY REFUND_DATE DESC) RANK, PA.MEMBER_NO FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN PAY PA ON (OD.ORDER_NO = PA.ORDER_NO) JOIN PAY_DETAIL PD ON (PA.PAY_NO = PD.PAY_NO) WHERE PA.MEMBER_NO = ? AND REFUND_STATUS IS NOT NULL))
selectListCountC=SELECT COUNT(*) FROM (SELECT ADJUST_NO, ADJUST_DATE, ADJUST_DIV, PRICE, ACCOUNT_HOLDER, BANK_CODE, ACCOUNT_NO, MEMBER_NO, RANK FROM (SELECT ADJUST_NO, ADJUST_DATE, ADJUST_DIV, PRICE, ACCOUNT_HOLDER, BANK_CODE, ACCOUNT_NO, A.MEMBER_NO, ROW_NUMBER() OVER (ORDER BY ADJUST_DATE DESC) RANK FROM ADJUSTMENT A JOIN MEMBER M ON (A.MEMBER_NO = M.MEMBER_NO) WHERE A.MEMBER_NO = ?))

selectRefundDateCount=SELECT COUNT(*) FROM (SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, REFUND_STATUS, RANK, MEMBER_NO FROM (SELECT OD.ORDER_NO, PD.REFUND_DATE, P.PRODUCT_NAME, OD.PRODUCT_DTPAY, PD.REFUND_STATUS, ROW_NUMBER() OVER (ORDER BY REFUND_DATE DESC) RANK, PA.MEMBER_NO FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN PAY PA ON (OD.ORDER_NO = PA.ORDER_NO) JOIN PAY_DETAIL PD ON (PA.PAY_NO = PD.PAY_NO) WHERE PA.MEMBER_NO = ? AND REFUND_STATUS IS NOT NULL)) WHERE REFUND_DATE BETWEEN TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND TO_CHAR(TO_DATE(?,'YYYY-MM-DD'))
selectRefundDate=SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, REFUND_STATUS, RANK, MEMBER_NO FROM (SELECT OD.ORDER_NO, PD.REFUND_DATE, P.PRODUCT_NAME, OD.PRODUCT_DTPAY, PD.REFUND_STATUS, ROW_NUMBER() OVER (ORDER BY REFUND_DATE DESC) RANK, PA.MEMBER_NO FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN PAY PA ON (OD.ORDER_NO = PA.ORDER_NO) JOIN PAY_DETAIL PD ON (PA.PAY_NO = PD.PAY_NO) WHERE PA.MEMBER_NO = ? AND REFUND_STATUS IS NOT NULL) WHERE REFUND_DATE BETWEEN TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND RANK BETWEEN ? AND ?
selectCalculateDateCount=SELECT COUNT(*) FROM (SELECT ADJUST_NO, ADJUST_DATE, ADJUST_DIV, PRICE, ACCOUNT_HOLDER, BANK_CODE, ACCOUNT_NO, MEMBER_NO, RANK FROM (SELECT ADJUST_NO, ADJUST_DATE, ADJUST_DIV, PRICE, ACCOUNT_HOLDER, BANK_CODE, ACCOUNT_NO, A.MEMBER_NO, ROW_NUMBER() OVER (ORDER BY ADJUST_DATE DESC) RANK FROM ADJUSTMENT A JOIN MEMBER M ON (A.MEMBER_NO = M.MEMBER_NO) WHERE A.MEMBER_NO = ?)) WHERE ADJUST_DATE BETWEEN TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND TO_CHAR(TO_DATE(?,'YYYY-MM-DD'))
selectCalculateDate=SELECT ADJUST_NO, ADJUST_DATE, ADJUST_DIV, PRICE, ACCOUNT_HOLDER, BANK_CODE, ACCOUNT_NO, MEMBER_NO, RANK FROM (SELECT ADJUST_NO, ADJUST_DATE, ADJUST_DIV, PRICE, ACCOUNT_HOLDER, BANK_CODE, ACCOUNT_NO, A.MEMBER_NO, ROW_NUMBER() OVER (ORDER BY ADJUST_DATE DESC) RANK FROM ADJUSTMENT A JOIN MEMBER M ON (A.MEMBER_NO = M.MEMBER_NO) WHERE A.MEMBER_NO = ?) WHERE ADJUST_DATE BETWEEN TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND RANK BETWEEN ? AND ?

selectPayNo=SELECT PD.PAY_NO FROM PAY P JOIN PAY_DETAIL PD ON (P.PAY_NO = PD.PAY_NO) WHERE P.ORDER_NO = ?
updateRefund=UPDATE PAY_DETAIL SET REFUND_STATUS = '1', REFUND_DATE = SYSDATE WHERE PAY_NO = ?
selectProduct=SELECT PRODUCT_CODE FROM ORDER_DETAIL WHERE ORDER_NO = ?
updateProduct=UPDATE PRODUCT_CHECK SET COMPLETE_STATUS = '2' WHERE PRODUCT_CODE = ?


selectSaleListCount=SELECT COUNT(*) FROM (SELECT P.POSTS_ID, P.PRODUCT_NAME, PD.PAY_STATUS, A.ADJUST_DIV, P.KEEP_DATE FROM PAY_DETAIL PD JOIN PAY PA ON (PD.PAY_NO = PA.PAY_NO) JOIN ORDER_DETAIL OD ON (PA.ORDER_NO = OD.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN ADJUSTMENT A ON (PD.PAY_DTNO = A.PAY_DTNO) WHERE P.MEMBER_NO = ?)
selectSaleList=SELECT POSTS_ID, PRODUCT_NAME, PAY_STATUS, ADJUST_DIV, KEEP_DATE, RANK FROM (SELECT P.POSTS_ID, P.PRODUCT_NAME, PD.PAY_STATUS, A.ADJUST_DIV, P.KEEP_DATE, ROW_NUMBER() OVER(ORDER BY POSTS_ID DESC) RANK FROM PAY_DETAIL PD JOIN PAY PA ON (PD.PAY_NO = PA.PAY_NO) JOIN ORDER_DETAIL OD ON (PA.ORDER_NO = OD.ORDER_NO) RIGHT OUTER JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) LEFT OUTER JOIN ADJUSTMENT A ON (PD.PAY_DTNO = A.PAY_DTNO) WHERE P.MEMBER_NO = ?) WHERE RANK BETWEEN ? AND ?

cancelLookList=SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, PAY_STATUS, RANK FROM (SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, PAY_STATUS, ROW_NUMBER() OVER(ORDER BY REFUND_DATE DESC) RANK FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN PAY PA ON (OP.ORDER_NO = PA.ORDER_NO) JOIN PAY_DETAIL PD ON (PA.PAY_NO = PD.PAY_NO) WHERE MEMBER_NO = ? AND PAY_STATUS = 2) WHERE RANK BETWEEN ? AND ?
cancelLookCount=SELECT COUNT(*) FROM (SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, PAY_STATUS, RANK FROM (SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, PAY_STATUS, ROW_NUMBER() OVER(ORDER BY REFUND_DATE DESC) RANK FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN PAY PA ON (OP.ORDER_NO = PA.ORDER_NO) JOIN PAY_DETAIL PD ON (PA.PAY_NO = PD.PAY_NO) WHERE MEMBER_NO = ? AND PAY_STATUS = 2))


selectCancelDate=SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, PAY_STATUS, RANK FROM (SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, PAY_STATUS, ROW_NUMBER() OVER(ORDER BY REFUND_DATE DESC) RANK FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN PAY PA ON (OP.ORDER_NO = PA.ORDER_NO) JOIN PAY_DETAIL PD ON (PA.PAY_NO = PD.PAY_NO) WHERE MEMBER_NO = ? AND PAY_STATUS = 2) WHERE RANK BETWEEN ? AND ? AND REFUND_DATE BETWEEN TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND TO_CHAR(TO_DATE(?,'YYYY-MM-DD'))
selectCancelDateCount=SELECT COUNT(*) FROM (SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, PAY_STATUS, RANK FROM (SELECT ORDER_NO, REFUND_DATE, PRODUCT_NAME, PRODUCT_DTPAY, PAY_STATUS, ROW_NUMBER() OVER(ORDER BY REFUND_DATE DESC) RANK FROM ORDER_PRODUCT OP JOIN ORDER_DETAIL OD ON (OP.ORDER_NO = OD.ORDER_NO) JOIN PRODUCT P ON (OD.PRODUCT_CODE = P.PRODUCT_CODE) JOIN PAY PA ON (OP.ORDER_NO = PA.ORDER_NO) JOIN PAY_DETAIL PD ON (PA.PAY_NO = PD.PAY_NO) WHERE MEMBER_NO = ? AND PAY_STATUS = 2) WHERE REFUND_DATE BETWEEN TO_CHAR(TO_DATE(?,'YYYY-MM-DD')) AND TO_CHAR(TO_DATE(?,'YYYY-MM-DD')))

updatePay=UPDATE PAY_DETAIL SET PAY_STATUS = '2', REFUND_DATE = SYSDATE WHERE PAY_NO = ?
